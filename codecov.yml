coverage:
  status:
    project:
      default: off
      api:
        flags:
          - api
        threshold: 10%
      front:
        flags:
          - front
        threshold: 10%
    patch: off

flags:
  api:
    paths:
      - api/
  front:
    paths:
      - src/
      - tests
